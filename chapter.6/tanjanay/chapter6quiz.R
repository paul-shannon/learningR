#Chapter 6 Quiz:

#1. What is another name for global environment?
# user workspace

#2. How would you convert a list to an environment?
# use the list2env function (with a list as the input variable). You can also use the as.environment function

#3. How do you print the contents of a function to the console?
# type out the name of the function in the console

#4. Name 3 functions that tell you the names of the formal arguents of a function.
#formals, args, and formalArgs

#5 What does the do.call function do?
#the "do.call" function allows you to pass arguments as a list rather than one at a time